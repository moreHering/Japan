---
import Base from '../layouts/Base.astro';
import PlaceExplorer from '../components/PlaceExplorer.svelte';
import { places } from '../lib/places';
---

<Base
  title="Orte"
  subtitle="Die 164 Orte des Reisebands und alles, was ihr selbst ergänzt — filterbar, auf der Karte und direkt auf einen Reisetag legbar."
>
  <PlaceExplorer client:only="svelte" places={places} />
</Base>
